/**
 * ค่าตั้งต้นของระบบ
 *
 * ⚠️ ค่าที่ต้องแก้ก่อนใช้งานจริง: ORG_NAME, ORG_CONTACT, LINE_OA_ID, BASE_URL
 * แก้ได้ 2 ทาง: แก้ค่า default ตรงนี้ หรือใส่ตัวแปรใน .env (แนะนำ)
 */

module.exports = {
  // ชื่อบริการที่แสดงต่อผู้ใช้
  siteName: process.env.SITE_NAME || 'Safeโอน (SafeOne)',
  slogan: 'โอนปลอดภัย ทุกยอดเงิน',

  // โดเมนจริงหลัง deploy เช่น https://checkbeforepay.up.railway.app
  // ใช้สร้าง sitemap.xml และลิงก์ og: ให้ Google เก็บหน้าเว็บได้ถูกต้อง
  baseUrl: (process.env.BASE_URL || '').replace(/\/+$/, ''),

  // ชื่อผู้ควบคุมข้อมูลส่วนบุคคล (บุคคล กลุ่ม หรือหน่วยงานที่ดูแลระบบนี้)
  orgName: process.env.ORG_NAME || '[ยังไม่ได้ระบุชื่อผู้ดูแลระบบ]',

  // ช่องทางติดต่อสำหรับขอแก้ไข/ลบข้อมูล หรือถอนความยินยอม
  orgContact: process.env.ORG_CONTACT || '[ยังไม่ได้ระบุช่องทางติดต่อ]',

  // ระยะเวลาเก็บข้อมูล
  retention: process.env.DATA_RETENTION || 'ตลอดเวลาที่ผู้ขายยังอยู่ในระบบ และลบภายใน 30 วันหลังถอนความยินยอม',

  // ไอดี LINE Official Account เช่น @123abcde (ดูได้ในแท็บ Messaging API)
  lineOaId: (process.env.LINE_OA_ID || '@206jnkap').trim(),

  /**
   * ประเภทของผู้ขายที่ตรวจสอบได้
   * ระบบไม่ผูกกับที่พักหรือพื้นที่ใดพื้นที่หนึ่งอีกต่อไป
   * เลขบัญชีที่คนโดนหลอกให้โอน มาจากธุรกิจแบบไหนก็ได้
   */
  categories: [
    'ที่พัก / รีสอร์ท',
    'ร้านค้าออนไลน์',
    'รถเช่า / รถตู้',
    'ทัวร์ / กิจกรรม',
    'ตั๋ว / บัตรคอนเสิร์ต',
    'พรีออเดอร์ / หิ้วของ',
    'อื่น ๆ',
  ],

  // เวอร์ชันของข้อความยินยอม — ถ้าแก้เนื้อหาหนังสือยินยอม ต้องขยับเลขนี้ด้วย
  consentVersion: 'v1.1',
};
